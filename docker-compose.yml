services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      # Host and container path must match — see DooD note in api/jobs.py.
      - JOB_OUTPUT_DIR=${PWD}/backend/media/jobs
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PWD}/backend/media/jobs:${PWD}/backend/media/jobs
